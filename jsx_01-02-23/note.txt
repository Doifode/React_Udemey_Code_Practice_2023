First installed the node.js by using the browser
Created the app by using npx create-react-app command 
Then Created the Simple app with basic of the files 

in index.js file 
first import the React Through react liabrary 
and  the ReactDom through ReactDom/client 

To Write html we create constant and  get the control of the  root element from the index.html and then we create the root in that
in that element by using ReactDom.creatroot() key word

then the function compnent is created and return some jsx through that component
and lastly the component that we created is rendered using  render keyword

